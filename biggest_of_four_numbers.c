#include<stdio.h>
void main()
{
    int a,b,c,d;
    printf("Enter a,b,c,d values");
    scanf("%d%d%d%d",&a,&b,&c,&d);
    if(a>b)
    if(a>c)
    if(a>d)
    printf("%d is bigger than remaining",a);
}